# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NewDawn' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NewDawn
  	pod 'Charts', :inhibit_warnings => true
	pod 'PDFGenerator', '~> 2.1', :inhibit_warnings => true
	pod 'JTAppleCalendar', '~> 7.0', :inhibit_warnings => true
	pod 'Firebase/Core', :inhibit_warnings => true
	pod 'Firebase/Auth', :inhibit_warnings => true
pod 'Firebase/Storage', :inhibit_warnings => true
pod 'Firebase/Database', :inhibit_warnings => true


	pod 'FBSDKLoginKit', :inhibit_warnings => true
	pod 'TwitterKit', :inhibit_warnings => true
	pod 'GoogleSignIn', :inhibit_warnings => true
  target 'NewDawnTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
